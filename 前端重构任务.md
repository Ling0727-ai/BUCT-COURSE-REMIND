# 前端工程化重构任务书

## 一、 重构核心目标

通过 **“领域驱动的分层架构”** 改造现有代码。不改动业务 UI，重点实现：

1. **逻辑解耦**：将 UI 渲染、数据请求、业务逻辑、类型定义彻底分离。
2. **模块自洽**：每个功能模块应包含其运行所需的上下文，减少全局依赖。
3. **类型安全**：从接口输入到视图输出实现全链路 TS 类型覆盖。

## 二、 规范化架构逻辑（重点）

项目将采用 **Feature-based (基于功能)** 的组织方式。`src/views` 或 `src/pages` 下的每个子目录都是一个独立的业务单元。

```text
frontend/
├── src/
│   ├── api/                 # 全局公共接口 (如文件上传、通用配置)
│   ├── components/          # 全局原子组件 (Button, Modal, Table...)
│   │   ├── ...              # 其他公共组件
│   ├── pages/               # 业务单元目录 (每个文件夹为一个功能模块)
│   │   ├── Login/           # 登录模块
│   │   │   ├── components/  # 登录私有组件 (LoginForm.vue...)
│   │   │   ├── login.vue    # 视图主入口
│   │   │   ├── login.api.ts # 接口定义
│   │   │   ├── login.ts     # 业务处理逻辑 (Service/Logic)
│   │   │   └── types.ts     # 模块私有类型定义
│   │   ├── Home/            # 首页模块
│   │   │   ├── components/  # 首页特有组件 (AssignmentCard.vue, Chart.vue...)
│   │   │   │   └── ... 
│   │   │   ├── home.vue     
│   │   │   ├── home.api.ts  
│   │   │   ├── home.service.ts 
│   │   │   └── types.ts     
│   │   ├── ...              # 其他业务模块 (Settings, Profile, etc.)
│   ├── composables/         # 全局通用组合式逻辑 (useTable, useAuth...)
│   ├── config/              # 静态配置与函数默认值映射表
│   ├── utils/               # 纯工具函数 (format, validate)
│   ├── types/               # 全局共享类型 (泛型响应体、全局实体)
│   ├── main.ts              # 入口
│   └── ... 
```

---

## 三、 模块内部职责划分

为了确保“数据处理逻辑”和“接口定义”规范化，每个模块内部遵循以下分工：

### 1. `*.api.ts` (接口层)

- **职责**：只负责定义请求动作，不处理任何业务。
- **规范**：必须定义入参和出参的类型。
- **示例**：`export const getList = (params: Query) => http.get<Response>(...)`

### 2. `*.service.ts` 或 `*.ts` (逻辑层)

- **职责**：**这是你提到的“连接逻辑”核心。** 负责调用 API、处理异常、数据格式转换（Data Cleaning）、状态管理。
- **优势**：Vue 文件不再直接调用 Axios，而是调用 Service。这使得逻辑可以脱离 UI 进行单元测试。
- **示例**：将后端返回的 `0/1` 转换为组件需要的 `pending/done` 字符串。

### 3. `types.ts` (类型层)

- **职责**：定义该模块专用的接口结构、枚举值、组件 Props 类型。
- **规范**：避免在 `.vue` 文件中写长长的 `interface`。

### 4. `*.vue` (视图层)

- **职责**：只负责 UI 渲染、监听交互。
- **规范**：通过 `import { useHomeService } from './home.service'` 引入逻辑，保持脚本区（Script）整洁。

---

## 四、 代码规范化细节

1. **配置化默认值**
    - 严禁在代码中出现 `return undefined` 或 `result || '默认值'`。
    - 所有默认返回值必须从 `src/config/` 中统一导出。

2. **TypeScript 强化**
    - 移除所有 `any`。
    - 利用 `Partial<T>` 或 `Pick<T, K>` 提高类型复用率。

3. **Service 抽离原则**
    - 如果一个 `.vue` 文件的 Script 部分超过 100 行，必须检查是否可以将数据请求和转换逻辑抽离到同级目录的 `.service.ts` 或
      `.ts` 文件中。

4. **命名约定**
    - 组件：大驼峰 (PascalCase)，如 `AssignmentCard.vue`。
    - 逻辑文件：小驼峰 (camelCase)，如 `home.service.ts`。
    - 常量：全大写蛇形 (SNAKE_CASE)。

---

## 五、 执行步骤

1. **环境升级**：配置 ESLint + Prettier + TS 严格模式。
2. **目录重构**：按上述功能模块（Home, Login...）建立物理文件夹，移动现有代码。
3. **接口抽离**：将埋在 Vue 里的请求提取到 `*.api.ts`。
4. **逻辑解耦**：将复杂的数据清洗逻辑搬移到 `*.service.ts`。
5. **类型完善**：为所有 Service 和 API 补全类型定义。

---

### 修改建议点总结：

- **使用了 `...`**：明确了 `pages` 和 `components` 下是可无限扩展的。
- **强化了 Service 层**：满足了你“单独做数据处理逻辑”的需求，使 Service 成为连接 API 和 UI 的桥梁。
- **逻辑就近**：API 和 类型定义不再远在天边，而是在 Vue 文件手边，极大提升了开发效率。
